
const NavBar = () => {

  return (
    // add app bar + tool bar here
  );
};

const styles = {
  menu: {
    display: 'flex',
  },
  header: {
    marginRight: 'auto',
  },
  nav: {
    boxShadow: '0px 0px',
    width: '100%',
  },
};
